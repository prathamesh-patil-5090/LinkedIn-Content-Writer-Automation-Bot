# LinkedIn Daily Poster — web (public vars only)
# Copy to apps/web/.env.local
#   cp apps/web/.env.example apps/web/.env.local

# Next.js listen port (default 3000)
WEB_PORT=3000

# Must match apps/api/.env API_PORT
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
